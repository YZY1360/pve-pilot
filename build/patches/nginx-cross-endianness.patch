--- a/auto/endianness
+++ b/auto/endianness
@@ -32,7 +32,8 @@
 eval "$ngx_test >> $NGX_AUTOCONF_ERR 2>&1"
 
 if [ -x $NGX_AUTOTEST ]; then
-    if $NGX_AUTOTEST >/dev/null 2>&1; then
+    # 交叉编译：aarch64 / x86_64 目标均为小端，跳过运行直接声明
+    if [ -n "$PVE_NGINX_CROSS" ] || $NGX_AUTOTEST >/dev/null 2>&1; then
         echo " little endian"
         have=NGX_HAVE_LITTLE_ENDIAN . auto/have
     else
